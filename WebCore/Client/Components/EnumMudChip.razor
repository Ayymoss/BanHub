@if (_enumDetails.TryGetValue(Penalty, out var details))
{
    <RadzenBadge BadgeStyle="@details" Shade="Shade.Lighter" IsPill="true" Variant="Radzen.Variant.Flat" 
                 Style="padding: 0.2rem 0.4rem 0.10rem 0.4rem" Text="@Penalty.ToString()"/>
}

